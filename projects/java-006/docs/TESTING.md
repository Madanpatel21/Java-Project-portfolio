# JAVA-006 — Testing Guide
```bash
mvn test                      # 11 tests
mvn verify -Pstatic-analysis  # + Checkstyle + SpotBugs (Max effort)
```
Suites: ApprovalFlowIT (5) · SecurityIT (5) · PostgreSQLMigrationIT (CI).
