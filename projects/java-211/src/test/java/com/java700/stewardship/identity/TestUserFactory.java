package com.java700.stewardship.identity;

/** Marker package for test fixtures (kept separate from production identity concerns). */
public final class TestUserFactory {

    private TestUserFactory() {
    }
}
